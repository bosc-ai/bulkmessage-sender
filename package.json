{
  "name": "bulkmessagesender-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "BulkMessageSender marketing site + Markdown content build (blog, articles, help, resources).",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
